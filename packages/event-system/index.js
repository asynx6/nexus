// @nexus/event-system — EventBus + append-only EventStore (JSONL + SQLite index), replay iterator
// public facade: export ONLY contracts here (see docs/ARCHITECTURE.md rule 4)
export const NAME = '@nexus/event-system';
